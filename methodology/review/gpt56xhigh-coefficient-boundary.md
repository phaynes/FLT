# GPT-5.6 xhigh review: Taylor coefficient boundary

Candidate: `4d83061fb62a4eb98b33b47015161784ad9c31c8`

Model: `gpt-5.6-sol`, reasoning `xhigh`. Mode: fresh, read-only, bounded signature review. Approximate
elapsed wall time: eleven minutes.

Verdict: **REVISE PROVISIONAL COEFFICIENT BOUNDARY**.

The review independently reproduced the standard-trio axiom audit and agreed that conjugacy by a
linear equivalence is the correct representation isomorphism. It found the same residual-map
defect as Fable. Its suggested spelling was an `IsLocalHom (algebraMap R k)` instance. For a local
source and field target this enforces the same maximal-ideal kernel boundary; the producer selected
the explicit ideal containment so the mathematical condition remains visible in the proposition.

The review also found no pinned Mathlib Brauer--Nesbitt terminal. The uniqueness theorem therefore
remains an explicit graph obligation rather than a property assumed by the interface.
