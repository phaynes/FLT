# Fable 5 second bounded repair — cyclic base change

Act as the scarce independent diversity repairer at difficulty 10. Work read-only in
`/Volumes/second-store/devel/proof-forks/FLT` at commit
`dca95b766b94651010f3859b747e0ee19f046abc`. Do not edit repository files, task state, graph rows,
source registers, or Lean sources. Temporary Lean probes outside the repository are allowed.

Read in full:

- `methodology/review/flt-completion/cyclic-base-change/stage-1-opus48-primary.md`;
- `methodology/review/flt-completion/cyclic-base-change/stage-2-gpt56xhigh-review.md`;
- `methodology/review/flt-completion/cyclic-base-change/stage-3-fable5-diversity-repair.md`;
- `methodology/review/flt-completion/cyclic-base-change/stage-4-opus48-synthesis.md`;
- `methodology/review/flt-completion/cyclic-base-change/stage-5-gpt56xhigh-synthesis-review.md`;
- every cited live declaration, source row, consumer and graph row.

Repair only the defects established by the independent review:

1. Remove the unsupported claim that all future consumers are `Type 0`. Distinguish the fact that
   there are no current applications from any claim about absent future consumers. Do not mutate or
   weaken the frozen two-universe public theorem.
2. Do not retain the false broad `BaseChangeFiberUpToTwist`. Either delete it from the bounded slice
   and expose the exact missing source theorem as an open provider, or replace it by a precise
   conditional interface whose hypotheses are sufficient for Clifford/Schur theory: finite Galois
   extension, appropriate coefficient field/algebraic closure, finite dimensionality, and the exact
   irreducibility condition after restriction. Replay any replacement signature in Lean and do not
   call an elaborating `Prop` a proved theorem.
3. Use the counterexample `rho = 1 + epsilon`, `sigma = 1 + 1` over a quadratic extension as a
   mandatory regression against any repaired fiber statement.
4. Reconcile Jacquet--Langlands ownership consistently in both directions. State the complete open
   analytic provider set: forward base change, image/descent, cuspidality, Jacquet--Langlands, strong
   multiplicity one, and level/conductor compatibility.
5. Preserve the independent review's exact result that only four structural theorems are presently
   safe to bank: `even_finrank_of_even_base`, `mem_preimageComapFinset_iff`,
   `natCast_notMem_of_mem_preimageComapFinset`, and `heckeAlgebra_algHom_ext`, each with only the
   standard axiom trio. They are infrastructure, not `FLT-CBASE` proof progress.
6. Give exact binder-complete signatures for every retained proposed definition or theorem. Remove
   V6/V7/V9 unless they are fully stated and replayed. No source locator may be upgraded beyond the
   incomplete live register.
7. Produce the smallest corrected dependency graph and the exact next production/review gate. No
   graph mutation, public-theorem mutation, T2 registration, `knownin1980s`, custom axiom, build, or
   `FLT-CBASE` promotion is authorized.

Return exactly one verdict: `DESIGN-VIABLE`, `REVISE`, `OBSTRUCTION`, or `NO-RESULT`. This is the
second and final bounded diversity-repair iteration; if the exact statement still cannot be made
source-faithful, return `OBSTRUCTION` rather than widening the scope or hiding the theorem.
