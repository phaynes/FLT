# Stage 2 prompt — GPT-5.6 xhigh independent auxiliary/residual-image review

Difficulty 10. Fresh independent read-only review. Do not edit the repository, graph, source
register, Lean files, task state, or review artifacts.

Read:

- `methodology/review/flt-completion/auxiliary-residual-image/stage-1-prompt-opus48-primary.md`
- `methodology/review/flt-completion/auxiliary-residual-image/stage-1-opus48-primary.md`
- the exact obligation, graph, source-design, and source-register rows for
  `FLT-AUX-LOCAL-FIELD`, `FLT-AUX-CURVE`, and `FLT-RESIDUAL-IMAGE`;
- the Moret--Bailly, class-field, modularity-lifting, p-adic-Hodge, and induced-modularity
  producer/consumer artifacts cited by the Opus design;
- the exact `GaloisRep.map`, `GaloisRep.IsIrreducible`, `WeierstrassCurve.galoisRep`,
  `CyclotomicField`, `IntermediateField.LinearDisjoint`, and good-reduction APIs in this pin.

Hostilely adjudicate the Opus `UNCERTAIN` design. In particular:

1. verify whether the three-way split into field conditions, auxiliary-curve existence, and
   cyclotomic-restriction irreducibility is complete and acyclic;
2. ensure ordinary residual irreducibility, irreducibility after restriction to `F(ζℓ)`, and
   Taylor--Wiles adequacy are never conflated;
3. verify the source really requires unramified rather than split-completely local conditions;
4. check that good reduction is curve-relative and is not hidden in a field-only predicate;
5. check the two independent residual applications and whether either requires a separate node;
6. determine whether class-field theory is genuinely a dependency of the concrete T1 residual
   route or only of a later T2 induced-character provider;
7. test every currently buildable proposed signature with temporary Lean probes, replacing every
   schematic `True` or comment-placeholder with either an exact type or a named unresolved gate;
8. reject any interface whose apparent elaboration depends on `sorry`, a fabricated source locator,
   a generic `Prop`, or an axiom hidden inside a structure field;
9. give the transitively reduced graph mutation, if any, but do not apply it;
10. identify the smallest kernel-clean unit that can land now and the first genuine mathematical
    theorem after that unit.

Apply the typed diversity rule exactly. Return one of `PASS`, `REVISE`, `UNCERTAIN`, or `REFUTED`.
If not PASS, classify the defects as either:

- `SUBSTANTIVE-MATHEMATICAL-OR-STATEMENT-LEVEL`, which triggers the conditional Fable pass because
  this component has difficulty 10; or
- `MECHANICAL-OR-SCOPE-ONLY`, which does not trigger Fable.

Give exact accepted Lean signatures, explicit gate ownership, source status, dependency orientation,
the first buildable probe, and the first expected Lean residual. No promotion is authorized by the
review itself.

