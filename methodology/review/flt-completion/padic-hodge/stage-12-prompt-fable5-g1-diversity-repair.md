FABLE 5 DIVERSITY REPAIR — P-ADIC-HODGE G1 CRYSTALLINE INFRASTRUCTURE

Work read-only at difficulty 10. Produce an independent repair of the bankable G1 layer after the
substantive GPT review. Do not edit repository files, control rows, graph rows, or task state.

Read:

- `methodology/review/flt-completion/padic-hodge/stage-6-pinned-library-gap-audit.md`;
- `methodology/review/flt-completion/padic-hodge/stage-9-opus48-g1-crystalline-design.md`;
- `methodology/review/flt-completion/padic-hodge/stage-11-gpt56xhigh-g1-decomposition-review.md`;
- the exact pinned period-ring, tensor-action, fixed-point, local-place, completion, and coefficient APIs.

Repair, with disposable Lean probes:

1. add the exact prime/nonzero hypotheses making `IsPlaceAbove` non-vacuous;
2. give a scalar-compatible diagonal tensor-action signature with all required
   `SMulCommClass` assumptions and the exact standard-trio proof;
3. design and probe the first invariant-submodule construction, explicitly proving scalar closure
   rather than relying on missing typeclass synthesis;
4. choose a mathematically correct coefficient architecture: either first formalize `E = Q_l` and
   later extend, or define the finite-extension target over `K0 tensor E`. Do not identify fixed
   scalars with `K0` while tensoring over arbitrary `E`;
5. add explicit owner/provider nodes for the local `C_p` Galois-action bridge and the missing
   PD-envelope-of-an-ideal API;
6. correct the role of Fontaine’s `t`, and keep `B_cris^Gamma = K0` as a theorem provider rather
   than structure data;
7. state the smallest exact production slice, dependency order, first residual Lean goal, axiom
   gates, and stop-losses.

Every claimed buildable declaration must compile in a temporary probe and audit to exactly
`[propext, Classical.choice, Quot.sound]`. Return exactly one of `DESIGN-VIABLE`, `REVISE`, or
`OBSTRUCTION`. Do not define `IsCrystallineAt`, claim a comparison theorem, or promote the full
p-adic-Hodge obligation.
