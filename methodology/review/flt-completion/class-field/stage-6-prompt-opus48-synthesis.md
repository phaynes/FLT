# Opus 4.8 synthesis — class-field after retained Fable diversity

Act as the non-scarce Opus synthesis producer for `FLT-CLASS-FIELD` at difficulty 10. Work read-only. Read all Stage 1-5 artifacts in this directory, especially:

- `stage-3-opus48-repair.md`
- `stage-4-gpt56xhigh-review.md`
- `stage-5-fable5-diversity.md`

Synthesize one exact design. Preserve the Stage-5 corrections unless repository evidence refutes them:

1. The immediate compiled MLT consumers use `localTameAbelianInertiaGroup` and only require the elementary tame-residue kernel boundary; do not put the full local reciprocity isomorphism on that critical path.
2. Freeze the arithmetic-Frobenius normalization actually used by `IsAutomorphicOfLevel`; do not conflate it with an element-level uniformizer equality.
3. Bank only elaborated object/predicate definitions now: the two rank-one carrier predicates, principal ideles, and the idele-class quotient. Keep discreteness, profiniteness, Artin reciprocity, globalization, and pi-zero group structure as named gaps with exact owners.
4. Preserve the odd-order Grunwald-Wang restriction and keep Skinner-Wiles, induced-representation bookkeeping, automorphy, and curve existence outside this component.
5. Correct the component DAG and consumer ledger, but do not fabricate a CFT source locator or authorize T2.

Return:

- a single exact Lean signature set;
- the minimal dependency DAG and source/owner table;
- the first no-axiom probe/build slice;
- explicit residual gaps and counterexamples;
- a verdict `READY-FOR-GPT-REVIEW`, `REVISE`, or `OBSTRUCTION`.

Run temporary read-only Lean probes if useful. Do not edit the repository, promote an obligation, register an axiom, build production Lean, or perform git/task/control operations.
