INDEPENDENT GPT-5.6 XHIGH REVIEW — CLASS-FIELD PRIMARY DESIGN

Review `stage-1-opus48-primary.md` read-only against `FLT-CLASS-FIELD`, the source register,
dependency graph, actual potential-modularity consumers, existing local/global class-field APIs, and
the Opus telemetry artifact. Hostilely test the four-way split CF-LOCAL-RECIP, CF-GLOBAL-RECIP,
CF-CHAR-PRESCRIBE, and CF-COMPAT; confirm that `IsFiniteOrderCharacter` and
`HasPrescribedLocalComponents` are true, minimal, and the correct first elaborating slice. Reject a
bundle that hides reciprocity or character existence behind fields. Return `PASS`, `UNCERTAIN`,
`REVISE`, or `REFUTED`; identify any Fable trigger and the next exact signature probe/build unit.
