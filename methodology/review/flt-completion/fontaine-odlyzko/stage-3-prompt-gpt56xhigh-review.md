INDEPENDENT GPT-5.6 XHIGH REVIEW — FONTAINE–ODLYZKO DIVERSITY RESULT

Review, read-only, both:

- `stage-1-opus48-primary.md`
- `stage-1-fable5-alternative.md`

against `FLT-FONTAINE-ODLYZKO`, the exact live consumers, source register, target-stage policy, and
pinned Lean/Mathlib APIs. The Opus result was `UNCERTAIN`; Fable returned `DECOMPOSE-FIRST` and
proposed two named human-gated T2 assumptions plus three produced infrastructure components.

Hostilely adjudicate:

1. whether the proposed decomposition is mathematically complete without a discriminant table or
   Dickson classification;
2. whether the trivial-quotient orientation is correct at the exact consumer;
3. whether the claimed `e₂ = 9` branch really has a missing inertia-cyclicity premise or proof;
4. whether each proposed T2 assumption is exact, source-locatable, independently typed, and strictly
   narrower than the old Odlyzko authority boundary;
5. whether each proposed produced subcomponent has an elaborating signature and no hidden
   independence, coefficient-field, quotient-orientation, or finite-flat assumption;
6. the smallest kernel-buildable first unit and the first likely residual goal.

Return exactly one leading verdict: `PASS`, `REVISE`, `UNCERTAIN`, or `REFUTED`. State whether Opus
and Fable have enough agreement to freeze a repaired component graph. Do not edit repository files,
register axioms, or promote an obligation. A human gate remains mandatory for every axiomisation.
