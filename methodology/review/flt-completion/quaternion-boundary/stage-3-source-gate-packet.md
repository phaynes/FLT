# Mandatory source gate — quaternion stabilizer finiteness

Status: **AUTHORIZED-MODERN-SYNTHESIS** (operator authorization: philip.haynes, 2026-07-17).

The proposed stabilizer-level interface elaborates, supplies the real consumer definitionally, and
its probe audit is exactly `[propext, Classical.choice, Quot.sound]`. Both Opus and GPT nevertheless
found the same provenance gap: the registered sources do not contain an exact primary pre-1990
theorem and printed page covering both the adelic-to-order construction and the exact scalar-kernel
bridge.

The human gate may proceed only after a reviewer verifies a primary source proving finiteness of

```text
(U A_f^× ∩ g⁻¹ D^× g) / F^×
```

for the exact totally-real, totally-definite, compact-level setting used by the consumer. The current
`hUo` openness hypothesis appears redundant for stabilizer finiteness and should be removed unless
the eventual primary theorem genuinely requires it.

Before the operator disposition below, the secondary Voight references supported the terminal
order-unit finiteness step but did not close the two missing bridges. In that blocked state, no T2
assumption could be registered from the packet.

## Operator disposition and application audit

The operator dispositioned the unavailable pre-1990 single-source condition after three independent
reviews agreed that no such theorem states the exact adelic packaging. The registered boundary is
therefore explicitly a **modern-synthesis T2 axiom**, faithfully sourced to Voight 2021, rather than
mislabelled as a pre-1990 historical theorem. The full provenance dossier is
`stage-4-provenance-disposition.md`.

The application gate passed:

- `FLT.AutomorphicForm.QuaternionAlgebra.Basic` rebuilt successfully;
- the exact hUo-free signature elaborated;
- `isFiniteRelIndex_Δ` discharges by applying the named axiom to `ℒ.U`,
  `ℒ.isCompact_U`, and `g`;
- the printed consumer term contains no `ℒ.isOpen_U`;
- all direct downstream consumers in `InnerProduct` and `HeckeOperators.Concrete` rebuilt;
- `#print axioms` on the consumer returned exactly
  `[propext, Classical.choice, Quot.sound,
  TotallyDefiniteQuaternionAlgebra.isFiniteRelIndex_stabilizer]`;
- generic `knownin1980s` is absent from this consumer path.

This transition authorizes and records the T2 boundary only. It does **not** discharge the T3
mathematical obligation.
