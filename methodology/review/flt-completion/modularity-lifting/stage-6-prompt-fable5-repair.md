# Stage 6 prompt — second and final Fable repair of modularity-lifting source boundary

Difficulty 10. This is the second agreement-gate diversity repair, triggered by a fresh substantive
GPT review after Opus synthesis. Work read-only. Do not edit repository files, task state, graph rows,
source registers, historical assumptions, or Lean sources. Temporary Lean probes outside the
repository are allowed.

Read the complete Stage-1 through Stage-5 modularity-lifting artifacts, the current coefficient and
p-adic-Hodge reviews, the auxiliary-residual-image review, and the exact live graph rows.

Repair only the four Stage-5 substantive defects while preserving the accepted five-unit repository
boundary unchanged:

1. replace ordinary residual irreducibility with an exact absolute-irreducibility predicate on the
   same selected model after extension to a named common residual algebraic closure;
2. connect regularity and the Fontaine--Laffaille interval to the actual extracted Hodge--Tate weights
   of the representation, making the Tier-2 extraction/equality provider explicit;
3. state residual agreement through explicit embeddings into the common residual closure unless
   identical residue-field ownership is independently proved;
4. introduce the smallest neutral absolute-residual vocabulary upstream of both the source contract
   and concrete residual-image theorem, and give the transitively reduced edge correction without
   making `FLT-MLT-SOURCE` consume its own downstream application proof.

Hostilely test ordinary-versus-absolute irreducibility, coefficient-field mismatches, arbitrary free
weight witnesses, and dependency cycles. Probe every accepted new signature and report exact
`#print axioms` results. Do not persist the five-unit probe merely because it is mechanically green;
the repaired source/ownership graph must also be statement-correct.

Return exactly one of `DESIGN-VIABLE`, `OBSTRUCTION`, or `NO-RESULT`, followed by exact Lean
signatures, dependency order, graph-edge replacements, source/consumer ownership, probe evidence,
remaining provider theorems, and the first residual Lean goal. This is the second and final automatic
agreement iteration; another substantive disagreement must go to the human tie-break rather than
starting an unbounded design loop.
